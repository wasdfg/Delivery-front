import React from "react";

function LoginPage() {
  return (
    <div>
      <h1>로그인</h1>
      {/* 나중에 여기에 이메일, 비밀번호 input과
          로그인 버튼을 만들고, 'useAuth' 훅을 사용해
          로그인 API를 호출할 것입니다. */}
    </div>
  );
}

export default LoginPage;
